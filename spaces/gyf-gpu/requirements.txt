spaces
gradio>=4.44
open_clip_torch>=2.24
torch
pillow
numpy
# Marqo-FashionSigLIP / SigLIP2 load their tokenizer via HF Transformers at
# runtime (open_clip.get_tokenizer); SigLIP's tokenizer needs sentencepiece+protobuf.
transformers>=4.40
sentencepiece
protobuf
# Skin-tone lane (M4): RetinaFace face detect + FaRL face-parsing (MIT) for the
# real CIELAB->MST pipeline vendored under skintone/. CPU-capable; torch already present.
pyfacer>=0.0.5
# Body-type lane (M3): SAM 3D Body → MHR mesh. Provides `notebook.utils.setup_sam_3d_body`,
# which app.py imports — WITHOUT this the body call raises ModuleNotFoundError on the Space
# (skin-tone still works). GATED: the HF account behind GYF_HF_TOKEN must request access to
# facebook/sam-3d-body-dinov3 AND accept the SAM license, or the checkpoint download 401s.
# PyTorch3D is the fragile part on ZeroGPU — pin a CUDA-matched prebuilt wheel if the source
# build times out / fails to compile.
hydra-core>=1.3
iopath
git+https://github.com/facebookresearch/pytorch3d.git@stable
git+https://github.com/facebookresearch/sam-3d-body.git
