spaces
gradio>=4.44
open_clip_torch>=2.24
torch
pillow
numpy
# Marqo-FashionSigLIP / SigLIP2 load their tokenizer via HF Transformers at
# runtime (open_clip.get_tokenizer); SigLIP's tokenizer needs sentencepiece+protobuf.
transformers>=4.40
sentencepiece
protobuf
# Skin-tone lane (M4): RetinaFace face detect + FaRL face-parsing (MIT) for the
# real CIELAB->MST pipeline vendored under skintone/. CPU-capable; torch already present.
pyfacer>=0.0.5
# Body-type lane (M3): commercial-clean, ZeroGPU-deployable silhouette pipeline
# (replaces SAM 3D Body — that stack needs detectron2+pyrender+pytorch3d+conda and
# is NOT pip-deployable on a Space; see docs/plans/m3-body-type-rtmw-birefnet.md).
#   - rtmlib: RTMW whole-body keypoints as plain ONNX (Apache-2.0, no mmcv/mmpose/mmdet)
#   - BiRefNet: SOTA high-res body silhouette mask (MIT)
# Mask widths at keypoint-anchored shoulder/waist/hip lines → ratios → silhouette taxonomy.
# NOTE: app.py estimate_body + ml/usermodel/body still need the matching rewrite before
# GYF_BODY_REMOTE_URL is pointed here — until then keep body unset (skin-tone unaffected).
rtmlib>=0.0.13
onnxruntime-gpu
# BiRefNet is loaded from HF (ZhengPeng7/BiRefNet) via transformers AutoModel + timm/kornia.
timm
kornia
