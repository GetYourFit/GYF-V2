spaces
gradio>=4.44
open_clip_torch>=2.24
torch
pillow
numpy
# Marqo-FashionSigLIP / SigLIP2 load their tokenizer via HF Transformers at
# runtime (open_clip.get_tokenizer); SigLIP's tokenizer needs sentencepiece+protobuf.
transformers>=4.40
sentencepiece
protobuf
# Skin-tone lane (M4): RetinaFace face detect + FaRL face-parsing (MIT) for the
# real CIELAB->MST pipeline vendored under skintone/. CPU-capable; torch already present.
pyfacer>=0.0.5
# Body-type lane (M3): commercial-clean, ZeroGPU-deployable silhouette pipeline
# (replaces SAM 3D Body — that stack needs detectron2+pyrender+pytorch3d+conda and
# is NOT pip-deployable on a Space; Sapiens is CC-BY-NC). app.py segments the body
# with BiRefNet (MIT) and returns the silhouette boundary as (x,y,0) vertices; the
# caller's existing usermodel.body geometry classifies the silhouette unchanged.
# BiRefNet loads from HF (ZhengPeng7/BiRefNet) via transformers AutoModel
# (trust_remote_code) and needs timm/kornia/einops; torchvision for preprocessing.
torchvision
timm
kornia
einops
