# External tooling (vendored plugin, has its own git history)
ECC/

# Dependencies
node_modules/
.pnp
.pnp.*
.yarn/*

# OS / editor
.DS_Store
*.pem
*.swp
.idea/
.vscode/

# Env / secrets
.env
.env.*
!.env.example

# Node / Next / build
.next/
out/
dist/
build/
.turbo/
.tmp/
.bun-cache/
coverage/
*.tsbuildinfo
*.log

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.mypy_cache/
.pytest_cache/
.ruff_cache/

# Terraform
.terraform/
*.tfstate
*.tfstate.*
terraform.tfvars
crash.log

# ML artifacts / data
*.ckpt
*.pt
*.safetensors
/data/
/models/
.cache/
.tools/

# Local scratch / tool caches (never commit)
.tmpcache/
.tmp/
.bun-tmp*/
.bun-cache/
.cache-local/

# Local ML model cache (HF weights / torch hub) — never committed
.hf-cache/
.torch-cache/

# Loose local test photos (body/skin/try-on inputs) — never commit user/test imagery
ml/*.jpg
ml/*.jpeg
ml/*.png

# Exported training datasets + reports (regenerable from the interactions spine)
ml/data/exports/

# Generated intermediate (api.ts is the committed artifact)
packages/types/openapi.json
.uv-cache/
.uv-python/
.vercel

# GAN design/build harness scratch (specs, eval reports, dev logs)
gan-harness/
.data/

# runtime event sink (local/dev fallback; prod uses postgres)
services/api/data/events.jsonl

# Local agent/skill tooling caches (ECC/caveman/ponytail) — machine-local, never committed
.agents/
.cache-gh/
skills-lock.json

# e2e fixture data (regenerable; exports already ignored above)
ml/data/e2e/
# Expo local state
/apps/expo/.expo/
