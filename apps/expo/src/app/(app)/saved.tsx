import { RoutePlaceholder } from "@/components/navigation/route-placeholder";

export default function SavedRoute() {
  return <RoutePlaceholder title="Saved" description="Saved looks are next." />;
}
