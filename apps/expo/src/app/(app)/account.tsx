import { RoutePlaceholder } from "@/components/navigation/route-placeholder";

export default function AccountRoute() {
  return <RoutePlaceholder title="Account" description="Account controls are next." />;
}
